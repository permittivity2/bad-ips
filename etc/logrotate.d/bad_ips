/var/log/bad_ips/bad_ips.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    create 0640 root root
    sharedscripts
    postrotate
        systemctl reload bad_ips.service >/dev/null 2>&1 || true
    endscript
}
